import 'package:flutter/material.dart';

class MovieTvDetail extends StatelessWidget{
  const MovieTvDetail({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Detay sayfası'),
      ),
    );
  }
}